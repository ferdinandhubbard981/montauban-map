Instead of existing poste number offset logic, just get the 'bisecting' gradient (with the same angle between both incoming lines), and move x along that line. This should improve performance as well.
